// Fill these in to store people in Supabase instead of this browser.
// Leaving them empty keeps everything in localStorage.
window.LABEL_CONFIG = {
  SUPABASE_URL: "",
  SUPABASE_ANON_KEY: "",
  // Gate for opening the app. Client-side only — it is not real security.
  PASSWORD: "Labelapp32",
};
